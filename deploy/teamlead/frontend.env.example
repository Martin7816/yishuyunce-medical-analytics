# Optional frontend overrides. Copy to frontend/.env if needed.
# Empty base URL keeps the Vite /api proxy pointed at the local Flask server.
VITE_API_BASE_URL=
VITE_API_PROXY_TARGET=http://127.0.0.1:5000
