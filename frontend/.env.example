# Empty means Vite dev-server proxy (/api -> 127.0.0.1:5000) is used.
VITE_API_BASE_URL=

# Default is api. Set to mock only for reproducible UI state checks.
VITE_TOP10_MODE=api
VITE_TOP10_MOCK_STATE=success
